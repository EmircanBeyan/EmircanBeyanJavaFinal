spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ogrenci
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server.port=${PORT:8080}